version: '3'

services:
  web:
    container_name: challenge2-web
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    depends_on:
      - api

  api:
    build: .
    ports:
      - "3000:3000"
